BUGS:
Fix yield so that the timer computes time until next period (instead of just resetting when the timer goes off)

FEATURES:
Find compute times for user apps, configurable period

TESTING:
Test that single application runs within deadline (Print out execution start, finish time)
Test that multiple applications run within deadline
Test preemption
Test for memory leaks
